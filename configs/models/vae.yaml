## LSTM-VAE model configuration

architecture:
  latent_dim: 5
  hidden_dims_encoder: [256, 128]
  hidden_dims_decoder: [128, 256]
  
beta: 1.0

optimizer:
  type: 'Adam'
  learning_rate: 0.0001
  betas: [0.9, 0.999]

generation:
  filter_size: null #9 # Placeholder, will be computed after threshold selection